<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow JIT compilation (required for V8 in Electron) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required by some Electron internals) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dyld environment variables (required by Electron) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Allow outbound network connections -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Allow camera access for Google Meet/Huddle video calls -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <!-- Allow microphone access for Google Meet/Huddle calls -->
    <key>com.apple.security.device.microphone</key>
    <true/>
    <!-- Allow audio input device access (required alongside microphone entitlement) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>