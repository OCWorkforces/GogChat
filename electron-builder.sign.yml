# Electron Builder Configuration - Code Signing Extensions
extends: electron-builder.yml

mac:
  hardenedRuntime: true
  entitlements: entitlements.mac.plist
  entitlementsInherit: entitlements.mac.inherit.plist
